@model GreedIsland.Models.Card

@{
    ViewData["Title"] = "Details";
}
<h2>Card Details</h2>

<div>
    <h4>@* TODO: Replace placeholder text with actual card properties *@</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Rarity</dt>
        <dd class="col-sm-10">@Model.Rarity</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>

        <dt class="col-sm-2">Image</dt>
        <dd class="col-sm-10">
            @* TODO: Insert Greed Island card images *@
            <img src="@Model.ImageUrl" width="200" />
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Back to List</a>
</div>